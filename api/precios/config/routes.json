{
  "routes": [
    {
      "method": "GET",
      "path": "/precios",
      "handler": "precios.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/precios/count",
      "handler": "precios.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/precios/:id",
      "handler": "precios.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/precios",
      "handler": "precios.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/precios/:id",
      "handler": "precios.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/precios/:id",
      "handler": "precios.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
